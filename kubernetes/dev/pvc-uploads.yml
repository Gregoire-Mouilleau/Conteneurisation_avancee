apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-uploads-dev
  namespace: gestion-produits-dev
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
